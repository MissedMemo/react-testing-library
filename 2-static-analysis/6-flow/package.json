{
  "scripts": {
    "flow": "flow",
    "lint": "eslint src",
    "prettify": "prettier \"**/*.+(js|jsx|json|css|ts|tsx)\"",
    "format": "yarn prettify --write",
    "validate": "yarn lint && yarn prettify --list-different && yarn flow"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.11.1",
    "eslint-config-prettier": "^3.3.0",
    "flow-bin": "^0.89.0",
    "prettier": "^1.15.3"
  }
}
